pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // sh 'echo "first step"'
                sh '''
                    df -h
                    ls -lah
                '''
            }
        }
    }
}